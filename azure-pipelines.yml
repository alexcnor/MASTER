trigger:
  - none

pr: none

pool:
  vmImage: ubuntu-latest

parameters:
  - name: client
    type: string
    default: 'null'
  - name: environment
    type: string
    default: 'null'
  - name: nodeNumber
    type: number
    default: 0
  - name: resourceGroup
    type: string
    default: 'null'
  - name: vmSize
    type: string
    default: 'null'

steps:
  - script: | 
      echo ${{parameters.client}}
    displayName: Print Parameters
  - string: |
      ls -la
      cat main.tf
